# ContractAtlas

ContractAtlas is a WCF contract analysis and documentation generation toolkit.

It scans C# source from `sln`, `csproj`, or directory input, extracts service/data contracts, and produces machine-readable artifacts that can be used to generate OpenAPI/Swagger documentation.
